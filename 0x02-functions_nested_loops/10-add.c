#include "main.h"
#include <stdio.h>
/**
 * add - prints alphabets
 * Description: programs returns sum of a and b
 * @a: input to function
 * @b: second in put to function
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
