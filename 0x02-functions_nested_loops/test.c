#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

/**
 * main - Generates random valid passwords for the
 *        program 101-crackme.
 *
 * Return: Always 0.
 */
int main(void)
{
	char s[180] = "come";
	char *d = "ls";
	size_t i = strlen(s);
	size_t a = 1;

	//s[i] = d[1];
	printf("%ld\n", i);
}
